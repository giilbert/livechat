[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
schemars = "1.2.0"
maf = { version = "0.1.0-alpha.6", features = ["typed"] }
serde = { version = "^1", features = ["derive"] }

[lib]
crate-type = ["cdylib"] # This is required for Cargo to build the library
